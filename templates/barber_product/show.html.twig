{% extends 'base.html.twig' %}

{% block title %}BarberProduct
{% endblock %}

{% block body %}
	<h1>BarberProduct</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ barber_product.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ barber_product.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ barber_product.description }}</td>
			</tr>
			<tr>
				<th>Photo</th>
				<img src="{{ asset('/images/' ~ barber_product.photo) }}" alt="image product"/>
			</tr>
		</tbody>
	</td>
</tr>
<tr>
	<th>Price</th>
	<td>{{ barber_product.price }}</td>
</tr>
<tr>
	<th>Stock</th>
	<td>{{ barber_product.stock }}</td>
</tr></tbody></table><a href="{{ path('barber_product_index') }}">back to list</a><a href="{{ path('barber_product_edit', {'id': barber_product.id}) }}">edit</a>{{ include('barber_product/_delete_form.html.twig') }}{% endblock %}
